@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Hesap Oluştur — Travelio</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <link href="https://fonts.googleapis.com/css2?family=Sora:wght@400;500;600;700;800&family=Inter:wght@400;500;600&display=swap"
          rel="stylesheet" />

    <style>
        :root {
            --navy: #0e2a3b;
            --navy-2: #12384c;
            --teal: #0fa3a3;
            --teal-dark: #0b7f80;
            --cream: #f7f5f0;
            --white: #ffffff;
            --ink: #0e2a3b;
            --muted: #5d7180;
            --line: #e7e2d8;
            --shadow-sm: 0 4px 18px rgba(14,42,59,.06);
            --shadow-md: 0 18px 50px rgba(14,42,59,.12);
            --r: 18px;
        }

        * {
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', system-ui, sans-serif;
            color: var(--ink);
            background: var(--cream);
            -webkit-font-smoothing: antialiased;
            margin: 0;
        }

        h1, h2, h3, h4, .brand {
            font-family: 'Sora', sans-serif;
        }

        .eyebrow {
            font-family: 'Sora', sans-serif;
            text-transform: uppercase;
            letter-spacing: .22em;
            font-size: .72rem;
            font-weight: 700;
            color: var(--teal-dark);
        }

        a {
            text-decoration: none;
        }

        .brand {
            font-weight: 800;
            font-size: 1.5rem;
            color: var(--navy);
            letter-spacing: -.5px;
            display: flex;
            align-items: center;
            gap: .5rem;
        }

            .brand .dot {
                width: 11px;
                height: 11px;
                border-radius: 50%;
                background: var(--teal);
                display: inline-block;
                box-shadow: 0 0 0 4px rgba(15,163,163,.18);
            }

        .btn-teal {
            background: var(--teal);
            color: #fff;
            border: none;
            border-radius: 999px;
            font-weight: 600;
            padding: .85rem 1.6rem;
            transition: .25s;
        }

            .btn-teal:hover {
                background: var(--teal-dark);
                color: #fff;
                box-shadow: 0 12px 26px rgba(15,163,163,.32);
            }

        /* AUTH */

        .auth {
            min-height: 100vh;
            display: flex;
        }

        .auth-row {
            flex: 1;
            display: flex;
            flex-wrap: wrap;
        }

        /* LEFT VISUAL */

        .auth-visual {
            flex: 1 1 50%;
            position: relative;
            overflow: hidden;
            color: #fff;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            min-height: 100vh;
            padding: 2.4rem 2.8rem;
            background: linear-gradient( 160deg, rgba(14,42,59,.72) 0%, rgba(14,42,59,.45) 45%, rgba(11,127,128,.6) 100% ), url('https://images.unsplash.com/photo-1523906834658-6e24ef2386f9?auto=format&fit=crop&w=1400&q=80') center/cover;
        }

            .auth-visual .brand {
                color: #fff;
            }

                .auth-visual .brand .dot {
                    box-shadow: 0 0 0 4px rgba(255,255,255,.18);
                }

        .visual-body {
            max-width: 30rem;
        }

            .visual-body .eyebrow {
                color: #7fe3e3;
            }

            .visual-body h1 {
                font-weight: 800;
                font-size: clamp(1.9rem,3vw,2.7rem);
                line-height: 1.12;
                letter-spacing: -.5px;
                margin: .6rem 0 .8rem;
            }

            .visual-body p {
                color: rgba(255,255,255,.88);
                font-size: 1.02rem;
                line-height: 1.6;
                margin: 0 0 1.6rem;
            }

        .perks {
            list-style: none;
            padding: 0;
            margin: 0;
        }

            .perks li {
                display: flex;
                align-items: center;
                gap: .7rem;
                margin-bottom: .7rem;
                font-size: .96rem;
                color: rgba(255,255,255,.95);
            }

            .perks .pc {
                flex: none;
                width: 28px;
                height: 28px;
                border-radius: 9px;
                background: rgba(255,255,255,.16);
                border: 1px solid rgba(255,255,255,.25);
                display: grid;
                place-items: center;
                color: #7fe3e3;
            }

        .visual-foot {
            font-size: .82rem;
            color: rgba(255,255,255,.6);
        }

        /* RIGHT FORM */

        .auth-form {
            flex: 1 1 50%;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .form-top {
            display: flex;
            justify-content: flex-end;
            padding: 1.6rem 2rem 0;
        }

        .back-link {
            display: inline-flex;
            align-items: center;
            gap: .4rem;
            color: var(--muted);
            font-weight: 600;
            font-size: .88rem;
            transition: .2s;
        }

            .back-link:hover {
                color: var(--teal-dark);
            }

        .form-wrap {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1.5rem 2rem 3rem;
        }

        .form-inner {
            width: 100%;
            max-width: 440px;
        }

            .form-inner .brand {
                display: none;
            }

            .form-inner .eyebrow {
                margin-bottom: .4rem;
            }

            .form-inner h2 {
                font-weight: 800;
                font-size: 1.9rem;
                letter-spacing: -.5px;
                color: var(--navy);
                margin: 0 0 .4rem;
            }

            .form-inner .lead {
                color: var(--muted);
                font-size: .94rem;
                margin: 0 0 1.6rem;
            }

        /* FORM */

        .field {
            margin-bottom: 1rem;
        }

            .field label {
                display: block;
                font-size: .8rem;
                font-weight: 600;
                color: var(--navy);
                margin-bottom: .35rem;
            }

        .input-wrap {
            position: relative;
        }

        .field input {
            width: 100%;
            border: 1px solid var(--line);
            border-radius: 12px;
            padding: .8rem 1rem;
            font-size: .94rem;
            color: var(--navy);
            background: #fff;
            outline: none;
            transition: .2s;
            font-family: inherit;
        }

            .field input::placeholder {
                color: #a7b2ba;
            }

            .field input:focus {
                border-color: var(--teal);
                box-shadow: 0 0 0 3px rgba(15,163,163,.12);
            }

            .field input.err {
                border-color: #d98b7d;
                box-shadow: 0 0 0 3px rgba(217,139,125,.14);
            }

        .toggle-eye {
            position: absolute;
            right: .7rem;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            color: var(--muted);
            cursor: pointer;
            padding: .3rem;
            display: grid;
            place-items: center;
        }

            .toggle-eye:hover {
                color: var(--teal-dark);
            }

        .err-msg {
            color: #c26a52;
            font-size: .78rem;
            margin-top: .3rem;
            min-height: 1rem;
            display: none;
        }

            .err-msg.show {
                display: block;
            }

        .hint {
            font-size: .76rem;
            color: var(--muted);
            margin-top: .4rem;
        }

        /* PASSWORD STRENGTH */

        .strength {
            display: flex;
            align-items: center;
            gap: .6rem;
            margin-top: .55rem;
        }

        .strength-bars {
            display: flex;
            gap: 4px;
            flex: 1;
        }

            .strength-bars i {
                height: 5px;
                flex: 1;
                background: var(--line);
                border-radius: 999px;
                transition: .25s;
            }

        .strength-label {
            font-size: .75rem;
            font-weight: 600;
            color: var(--muted);
            white-space: nowrap;
            min-width: 38px;
            text-align: right;
        }

        /* AGREEMENT */

        .agree {
            display: flex;
            align-items: flex-start;
            gap: .6rem;
            margin: .4rem 0 1.1rem;
        }

            .agree input {
                width: 18px;
                height: 18px;
                accent-color: var(--teal-dark);
                margin-top: 2px;
                flex: none;
                cursor: pointer;
            }

            .agree label {
                font-size: .85rem;
                color: var(--muted);
                line-height: 1.5;
            }

            .agree a {
                color: var(--teal-dark);
                font-weight: 600;
            }

                .agree a:hover {
                    text-decoration: underline;
                }

        .btn-submit {
            width: 100%;
            justify-content: center;
            display: flex;
            align-items: center;
            gap: .5rem;
        }

        /* DIVIDER */

        .divider {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin: 1.4rem 0;
            color: var(--muted);
            font-size: .82rem;
        }

            .divider::before,
            .divider::after {
                content: "";
                flex: 1;
                height: 1px;
                background: var(--line);
            }

        .btn-google {
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: .7rem;
            background: #fff;
            border: 1px solid var(--line);
            border-radius: 999px;
            padding: .8rem;
            font-weight: 600;
            color: var(--navy);
            font-size: .92rem;
            cursor: pointer;
            transition: .2s;
            font-family: inherit;
        }

            .btn-google:hover {
                border-color: var(--teal);
                box-shadow: var(--shadow-sm);
            }

        .login-line {
            text-align: center;
            margin-top: 1.4rem;
            font-size: .9rem;
            color: var(--muted);
        }

            .login-line a {
                color: var(--teal-dark);
                font-weight: 600;
            }

                .login-line a:hover {
                    text-decoration: underline;
                }

        /* SUCCESS */

        .success-state {
            text-align: center;
            padding: 1rem 0;
        }

            .success-state .sc {
                width: 70px;
                height: 70px;
                border-radius: 20px;
                background: rgba(15,163,163,.12);
                color: var(--teal-dark);
                display: grid;
                place-items: center;
                margin: 0 auto 1.3rem;
            }

            .success-state h2 {
                font-size: 1.6rem;
                margin-bottom: .5rem;
            }

            .success-state p {
                color: var(--muted);
                margin: 0;
            }

            .success-state .redir {
                margin-top: 1rem;
                font-size: .88rem;
                color: var(--teal-dark);
                font-weight: 600;
                display: inline-flex;
                align-items: center;
                gap: .5rem;
            }

        .spinner {
            width: 15px;
            height: 15px;
            border: 2px solid rgba(15,163,163,.3);
            border-top-color: var(--teal-dark);
            border-radius: 50%;
            animation: spin .7s linear infinite;
        }

        @@keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }

        /* RESPONSIVE */

        @@media(max-width:991px) {

            .auth-visual {
                flex: 1 1 100%;
                min-height: auto;
                padding: 1.6rem 1.6rem 2rem;
            }

            .visual-body {
                padding: 1.2rem 0 .4rem;
            }

                .visual-body h1 {
                    font-size: 1.6rem;
                }

            .perks {
                display: none;
            }

            .visual-foot {
                display: none;
            }

            .auth-form {
                flex: 1 1 100%;
                min-height: auto;
            }

            .form-top {
                display: none;
            }

            .form-wrap {
                padding: 2rem 1.4rem 3rem;
            }
        }
    </style>
</head>

<body>

    <div class="auth">

        <div class="auth-row">

            <!-- LEFT VISUAL -->
            <div class="auth-visual">

                <a class="brand" href="/">
                    <span class="dot"></span>
                    Travelio
                </a>

                <div class="visual-body">

                    <div class="eyebrow">
                        Travelio topluluğuna katıl
                    </div>

                    <h1>
                        Yeni Maceraların Burada Başlıyor
                    </h1>

                    <p>
                        Travelio hesabını oluştur, favori rotalarını kaydet,
                        turlarını yönet ve seyahat deneyimlerini paylaş.
                    </p>

                    <ul class="perks">

                        <li>
                            <span class="pc">
                                <svg width="15" height="15" viewBox="0 0 24 24"
                                     fill="none" stroke="currentColor"
                                     stroke-width="2.4"
                                     stroke-linecap="round"
                                     stroke-linejoin="round">
                                    <path d="M20.8 4.6a5.5 5.5 0 0 0-7.8 0L12 5.6l-1-1a5.5 5.5 0 1 0-7.8 7.8l1 1L12 21l7.8-7.6 1-1a5.5 5.5 0 0 0 0-7.8z" />
                                </svg>
                            </span>
                            Favori turlarını kaydet
                        </li>

                        <li>
                            <span class="pc">
                                <svg width="15" height="15" viewBox="0 0 24 24"
                                     fill="none" stroke="currentColor"
                                     stroke-width="2.4"
                                     stroke-linecap="round"
                                     stroke-linejoin="round">
                                    <path d="M3 7h18v12H3z" />
                                    <path d="M8 7V5a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2" />
                                </svg>
                            </span>
                            Rezervasyonlarını tek yerden yönet
                        </li>

                        <li>
                            <span class="pc">
                                <svg width="15" height="15" viewBox="0 0 24 24"
                                     fill="none" stroke="currentColor"
                                     stroke-width="2.4"
                                     stroke-linecap="round"
                                     stroke-linejoin="round">
                                    <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" />
                                </svg>
                            </span>
                            Yorum ve değerlendirmelerini paylaş
                        </li>

                    </ul>

                </div>

                <div class="visual-foot">
                    © 2026 Travelio · Dünyayı keşfetmeye hazır ol.
                </div>

            </div>


            <!-- RIGHT FORM -->
            <div class="auth-form">

                <div class="form-top">

                    <a class="back-link" href="/">
                        <svg width="16" height="16" viewBox="0 0 24 24"
                             fill="none" stroke="currentColor"
                             stroke-width="2"
                             stroke-linecap="round"
                             stroke-linejoin="round">
                            <line x1="19" y1="12" x2="5" y2="12" />
                            <polyline points="12 19 5 12 12 5" />
                        </svg>

                        Ana Sayfaya Dön
                    </a>

                </div>


                <div class="form-wrap">

                    <div class="form-inner">

                        <a class="brand mb-4" href="/">
                            <span class="dot"></span>
                            Travelio
                        </a>


                        <!-- FORM VIEW -->
                        <div id="formView">

                            <div class="eyebrow">
                                Travelio'ya Hoş Geldin
                            </div>

                            <h2>
                                Hesabını Oluştur
                            </h2>

                            <p class="lead">
                                Seyahatlerini planlamak ve yönetmek için birkaç bilgi yeterli.
                            </p>


                            <div class="row g-0"
                                 style="gap:0 .8rem;flex-wrap:nowrap">

                                <div class="field" style="flex:1">

                                    <label for="firstName">
                                        Ad
                                    </label>

                                    <div class="input-wrap">
                                        <input type="text"
                                               id="firstName"
                                               placeholder="Adınız"
                                               autocomplete="given-name">
                                    </div>

                                    <div class="err-msg" id="err-firstName">
                                        Lütfen adını gir.
                                    </div>

                                </div>


                                <div class="field" style="flex:1">

                                    <label for="lastName">
                                        Soyad
                                    </label>

                                    <div class="input-wrap">
                                        <input type="text"
                                               id="lastName"
                                               placeholder="Soyadınız"
                                               autocomplete="family-name">
                                    </div>

                                    <div class="err-msg" id="err-lastName">
                                        Lütfen soyadını gir.
                                    </div>

                                </div>

                            </div>


                            <div class="field">

                                <label for="email">
                                    E-posta
                                </label>

                                <div class="input-wrap">
                                    <input type="email"
                                           id="email"
                                           placeholder="ornek@email.com"
                                           autocomplete="email">
                                </div>

                                <div class="err-msg" id="err-email">
                                    Geçerli bir e-posta adresi gir.
                                </div>

                            </div>


                            <div class="field">

                                <label for="phone">
                                    Telefon
                                </label>

                                <div class="input-wrap">
                                    <input type="tel"
                                           id="phone"
                                           placeholder="+90 5XX XXX XX XX"
                                           autocomplete="tel">
                                </div>

                                <div class="err-msg" id="err-phone">
                                    Lütfen telefon numaranı gir.
                                </div>

                            </div>


                            <div class="field">

                                <label for="password">
                                    Şifre
                                </label>

                                <div class="input-wrap">

                                    <input type="password"
                                           id="password"
                                           placeholder="Şifrenizi oluşturun"
                                           autocomplete="new-password">

                                    <button type="button"
                                            class="toggle-eye"
                                            data-target="password"
                                            aria-label="Şifreyi göster">

                                        <svg width="18" height="18"
                                             viewBox="0 0 24 24"
                                             fill="none"
                                             stroke="currentColor"
                                             stroke-width="2"
                                             stroke-linecap="round"
                                             stroke-linejoin="round">
                                            <path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7z" />
                                            <circle cx="12" cy="12" r="3" />
                                        </svg>

                                    </button>

                                </div>


                                <div class="strength"
                                     id="strengthWrap"
                                     style="display:none">

                                    <div class="strength-bars">
                                        <i></i>
                                        <i></i>
                                        <i></i>
                                    </div>

                                    <span class="strength-label"
                                          id="strengthLabel">
                                    </span>

                                </div>


                                <div class="hint">
                                    En az 8 karakter, bir büyük harf ve bir rakam kullan.
                                </div>

                                <div class="err-msg" id="err-password">
                                    Şifren en az 8 karakter, bir büyük harf ve bir rakam içermeli.
                                </div>

                            </div>


                            <div class="field">

                                <label for="password2">
                                    Şifre Tekrar
                                </label>

                                <div class="input-wrap">

                                    <input type="password"
                                           id="password2"
                                           placeholder="Şifrenizi tekrar girin"
                                           autocomplete="new-password">

                                    <button type="button"
                                            class="toggle-eye"
                                            data-target="password2"
                                            aria-label="Şifreyi göster">

                                        <svg width="18" height="18"
                                             viewBox="0 0 24 24"
                                             fill="none"
                                             stroke="currentColor"
                                             stroke-width="2"
                                             stroke-linecap="round"
                                             stroke-linejoin="round">
                                            <path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7z" />
                                            <circle cx="12" cy="12" r="3" />
                                        </svg>

                                    </button>

                                </div>

                                <div class="err-msg" id="err-password2">
                                    Şifreler eşleşmiyor.
                                </div>

                            </div>


                            <div class="agree">

                                <input type="checkbox" id="agree">

                                <label for="agree">
                                    <a href="#">Üyelik Sözleşmesi</a>
                                    ve
                                    <a href="#">Gizlilik Politikası</a>'nı
                                    okudum ve kabul ediyorum.
                                </label>

                            </div>


                            <div class="err-msg"
                                 id="err-agree"
                                 style="margin-top:-.7rem;margin-bottom:.7rem">

                                Devam etmek için sözleşmeyi kabul etmelisin.

                            </div>


                            <button type="button"
                                    class="btn-teal btn-submit"
                                    onclick="submitForm()">

                                Hesap Oluştur

                            </button>


                            <div class="divider">
                                veya
                            </div>


                            <button type="button"
                                    class="btn-google"
                                    onclick="googleDemo()">

                                <svg width="18" height="18"
                                     viewBox="0 0 24 24">

                                    <path fill="#4285F4"
                                          d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92a5.06 5.06 0 0 1-2.2 3.32v2.77h3.57c2.08-1.92 3.27-4.74 3.27-8.1z" />

                                    <path fill="#34A853"
                                          d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84A11 11 0 0 0 12 23z" />

                                    <path fill="#FBBC05"
                                          d="M5.84 14.1a6.6 6.6 0 0 1 0-4.2V7.06H2.18a11 11 0 0 0 0 9.88l3.66-2.84z" />

                                    <path fill="#EA4335"
                                          d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1A11 11 0 0 0 2.18 7.06l3.66 2.84C6.71 7.31 9.14 5.38 12 5.38z" />

                                </svg>

                                Google ile devam et

                            </button>


                            <div class="login-line">

                                Zaten hesabın var mı?

                                <a href="/Account/Login">
                                    Giriş Yap
                                </a>

                            </div>

                        </div>


                        <!-- SUCCESS VIEW -->
                        <div id="successView" style="display:none">

                            <div class="success-state">

                                <div class="sc">

                                    <svg width="34" height="34"
                                         viewBox="0 0 24 24"
                                         fill="none"
                                         stroke="currentColor"
                                         stroke-width="2.4"
                                         stroke-linecap="round"
                                         stroke-linejoin="round">

                                        <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" />
                                        <polyline points="22 4 12 14.01 9 11.01" />

                                    </svg>

                                </div>

                                <h2>
                                    Hesabın başarıyla oluşturuldu.
                                </h2>

                                <p>
                                    Travelio'ya hoş geldin,
                                    <b id="successName">gezgin</b>!
                                </p>

                                <div class="redir">
                                    <span class="spinner"></span>
                                    Profiline yönlendiriliyorsun...
                                </div>

                            </div>

                        </div>

                    </div>

                </div>

            </div>

        </div>

    </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <script>

        /* PASSWORD SHOW / HIDE */

        document.querySelectorAll(".toggle-eye").forEach(btn => {

            btn.addEventListener("click", () => {

                const input = document.getElementById(btn.dataset.target);

                const showing = input.type === "text";

                input.type = showing ? "password" : "text";

                btn.innerHTML = showing
                    ? '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7z"/><circle cx="12" cy="12" r="3"/></svg>'
                    : '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"/><line x1="1" y1="1" x2="23" y2="23"/></svg>';

            });

        });


        /* PASSWORD STRENGTH */

        const pw = document.getElementById("password");
        const strengthWrap = document.getElementById("strengthWrap");
        const bars = strengthWrap.querySelectorAll("i");
        const strengthLabel = document.getElementById("strengthLabel");

        function scorePassword(v) {

            let s = 0;

            if (v.length >= 8)
                s++;

            if (/[A-Z]/.test(v) && /[a-z]/.test(v))
                s++;

            if (/\d/.test(v) && /[^A-Za-z0-9]/.test(v))
                s++;
            else if (/\d/.test(v))
                s = Math.max(s, 1);

            return Math.min(s, 3);
        }


        pw.addEventListener("input", () => {

            const v = pw.value;

            if (!v) {

                strengthWrap.style.display = "none";
                return;
            }

            strengthWrap.style.display = "flex";

            const s = scorePassword(v);

            const colors = [
                "#d98b7d",
                "#e0a83d",
                "#0fa3a3"
            ];

            const labels = [
                "Zayıf",
                "Orta",
                "Güçlü"
            ];

            bars.forEach((b, i) => {

                b.style.background =
                    i < s
                        ? colors[s - 1]
                        : "var(--line)";

            });

            strengthLabel.textContent =
                s > 0 ? labels[s - 1] : "";

            strengthLabel.style.color =
                s > 0 ? colors[s - 1] : "var(--muted)";

        });


        /* VALIDATION */

        function setErr(id, show) {

            document
                .getElementById("err-" + id)
                .classList.toggle("show", show);

            const input = document.getElementById(id);

            if (input) {

                input.classList.toggle("err", show);

            }
        }


        function isEmail(v) {

            return /^[^\s@@]+@@[^\s@@]+\.[^\s@@]+$/.test(v);

        }


        function strongPw(v) {

            return v.length >= 8 &&
                   /[A-Z]/.test(v) &&
                   /\d/.test(v);

        }


        function submitForm() {

            const fn =
                document.getElementById("firstName").value.trim();

            const ln =
                document.getElementById("lastName").value.trim();

            const em =
                document.getElementById("email").value.trim();

            const ph =
                document.getElementById("phone").value.trim();

            const p1 =
                document.getElementById("password").value;

            const p2 =
                document.getElementById("password2").value;

            const ag =
                document.getElementById("agree").checked;


            let ok = true;


            setErr("firstName", !fn);

            if (!fn)
                ok = false;


            setErr("lastName", !ln);

            if (!ln)
                ok = false;


            setErr("email", !isEmail(em));

            if (!isEmail(em))
                ok = false;


            setErr("phone", !ph);

            if (!ph)
                ok = false;


            setErr("password", !strongPw(p1));

            if (!strongPw(p1))
                ok = false;


            const mismatch =
                !p2 || p1 !== p2;

            setErr("password2", mismatch);

            if (mismatch)
                ok = false;


            setErr("agree", !ag);

            if (!ag)
                ok = false;


            if (!ok) {

                const firstErr =
                    document.querySelector(".err-msg.show");

                if (firstErr) {

                    firstErr.scrollIntoView({
                        behavior: "smooth",
                        block: "center"
                    });

                }

                return;
            }


            /* SUCCESS */

            document.getElementById("successName").textContent = fn;

            document.getElementById("formView").style.display = "none";

            document.getElementById("successView").style.display = "block";


            setTimeout(() => {

                window.location.href = "/Profile";

            }, 1800);

        }


        /* CLEAR ERROR */

        [
            "firstName",
            "lastName",
            "email",
            "phone",
            "password",
            "password2"
        ].forEach(id => {

            document
                .getElementById(id)
                .addEventListener("input", () => {

                    setErr(id, false);

                });

        });


        document
            .getElementById("agree")
            .addEventListener("change", () => {

                setErr("agree", false);

            });


        /* GOOGLE */

        function googleDemo() {

            const btn =
                document.querySelector(".btn-google");

            btn.innerHTML =
                '<span class="spinner"></span> Google\'a bağlanılıyor...';

            setTimeout(() => {

                window.location.href = "/Profile";

            }, 1400);

        }

    </script>

</body>
</html>